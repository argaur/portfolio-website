# V3 P0/P1/P2 Visual QA Fixes — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix all 22 P0/P1/P2 issues found in the 2026-06-06 Playwright visual QA audit of the v3 branch, from a JS crash on every navigation down to mobile layout problems.

**Architecture:** All changes are in `index.html` (inline `<style>` block + HTML + inline `<script>` block). `app.js` is a dead v2 controller wired to a DOM that no longer exists — removing it eliminates 5 issues at once. The real v3 controller lives in the inline `<script>`. `initStackDiagram` is the only function from `app.js` that is still needed; it gets extracted into the inline script.

**Tech Stack:** Plain HTML, Tailwind CDN, vanilla JS, no build step.

---

## File Modifications

| File | What changes |
|---|---|
| `index.html` lines 157, 180–197 | CSS fixes: nav-link-active scope, remove dead proj-card CSS block |
| `index.html` lines 235–273 | Mobile CSS: stats 2×2, skills/philosophy 1-col, projects arrow, experience grid, contact canvas hidden |
| `index.html` lines 320–345 | Header: remove `nav-link` class from CTA + logo buttons |
| `index.html` lines 347–378 | Home panel: headline font-size, stats container class → grid |
| `index.html` lines 382–543 | Experience: restructure timeline spine → inline dots, remove exp-projects divs |
| `index.html` lines 554–648 | Projects: rows 07/08 clickable, remove standalone arrows |
| `index.html` lines 652–667 | Skills: cell 01 spans lg:col-span-2 |
| `index.html` lines 670–739 | Philosophy: capstone amber accent, header paragraph spacing |
| `index.html` lines 741–782 | Credentials: remove cert hover, add education content, rebalance split |
| `index.html` lines 784–813 | Contact: remove `#contact-canvas` dead element |
| `index.html` lines 828–839 | Remove compat modal block + `#skills-graph-canvas` |
| `index.html` line 842 | Remove `<script src="app.js" defer></script>` |
| `index.html` lines 844–1189 | Inline script: add `initStackDiagram`, fix `renderStatic()` at animation end |

---

## Task 1 — Remove app.js and all dead DOM nodes (P0·P1: Issues 1, 2, 8, 25, 28)

**Files:** Modify `index.html`

- [ ] **Step 1: Remove the `<script src="app.js" defer></script>` tag**

  In `index.html` line 842, delete:
  ```html
  <script src="app.js" defer></script>
  ```

- [ ] **Step 2: Remove the app.js modal compat block (lines 828–837)**

  Delete this entire block:
  ```html
  <!-- app.js modal compat -->
  <div id="modal-overlay" hidden style="position:fixed;inset:0;z-index:140;">
    <div style="position:absolute;inset:0;background:rgba(10,10,14,0.94);" onclick="document.getElementById('modal-overlay').hidden=true"></div>
    <div style="position:absolute;inset:0;display:flex;align-items:center;justify-content:center;padding:16px;pointer-events:none;">
      <div id="modal-panel" style="position:relative;background:#1a1a22;border:1px solid #2a2a38;width:100%;max-width:760px;padding:48px;max-height:85vh;overflow-y:auto;pointer-events:auto;">
        <button id="modal-close" style="position:absolute;top:16px;right:16px;color:#9896a0;" onclick="document.getElementById('modal-overlay').hidden=true"><span class="material-symbols-outlined">close</span></button>
        <div id="modal-body"></div>
      </div>
    </div>
  </div>
  ```

- [ ] **Step 3: Remove `#skills-graph-canvas` (line 839)**

  Delete: `<canvas id="skills-graph-canvas" style="display:none" aria-hidden="true"></canvas>`

- [ ] **Step 4: Remove hidden `#contact-canvas` from Contact panel (line 809)**

  Delete from within the contact panel:
  ```html
  <!-- Hidden compat: app.js's initContactCanvas still needs this element to not error -->
  <canvas id="contact-canvas" style="display:none" aria-hidden="true"></canvas>
  ```

- [ ] **Step 5: Remove all `<div class="exp-projects" ...></div>` containers** (5 occurrences across accordion rows)

  Delete each of these lines:
  - `<div class="exp-projects" data-company="independent"></div>`
  - `<div class="exp-projects" data-company="jindal"></div>`
  - `<div class="exp-projects" data-company="onevalley"></div>`
  - `<div class="exp-projects" data-company="taccomacco"></div>`
  - `<div class="exp-projects" data-company="rsp"></div>`

- [ ] **Step 6: Remove the dead proj-card CSS block (lines 180–197)**

  Delete this entire comment + CSS block from the `<style>` tag:
  ```css
  /* ── Proj-card grid (app.js renders these) ──────────────────────────────── */
  .proj-grid { display: grid; grid-template-columns: 1fr; gap: 12px; }
  @media(min-width:640px) { .proj-grid { grid-template-columns: repeat(2, 1fr); } }
  @media(min-width:1024px) { .proj-grid { grid-template-columns: repeat(3, 1fr); } }

  /* proj-card: Stitch tonal-card design language */
  .proj-card { background: #1e2022; border: 1px solid #2a2a38; padding: 24px; cursor: pointer; transition: background 0.2s, border-color 0.2s; display: flex; flex-direction: column; justify-content: space-between; min-height: 180px; }
  .proj-card:hover { background: #242432; border-color: #c9a84c; }
  .proj-card--lead { grid-column: 1 / -1; border-left: 3px solid #c9a84c; min-height: 220px; }
  .proj-card-meta { display: flex; flex-wrap: wrap; gap: 6px; margin-bottom: 12px; }
  .proj-tag { font-family: 'Space Mono', monospace; font-size: 10px; letter-spacing: 0.08em; text-transform: uppercase; color: #c9a84c; }
  .proj-role-chip { font-family: 'DM Sans', sans-serif; font-size: 11px; color: #9896a0; padding: 0 6px; border-left: 1px solid #2a2a38; }
  .proj-title { font-family: 'Space Mono', monospace; font-size: 16px; font-weight: 700; color: #ece8e2; line-height: 1.35; margin-bottom: 10px; }
  .proj-desc { font-family: 'DM Sans', sans-serif; font-size: 14px; color: #9896a0; line-height: 1.6; flex-grow: 1; margin-bottom: 14px; }
  .proj-metrics { display: flex; flex-wrap: wrap; gap: 6px; padding-top: 12px; border-top: 1px solid #2a2a38; }
  .proj-metric { font-family: 'Space Mono', monospace; font-size: 10px; color: #c9a84c; background: rgba(201,168,76,0.08); padding: 3px 8px; border: 1px solid rgba(201,168,76,0.2); }
  .proj-cta { font-family: 'DM Sans', sans-serif; font-size: 12px; font-weight: 500; color: #c9a84c; margin-top: 8px; }
  .proj-card:hover .proj-cta { text-decoration: underline; }
  ```

---

## Task 2 — Extract initStackDiagram into inline script (P2: Issue 5)

**Files:** Modify `index.html` inline `<script>` block

- [ ] **Step 1: Add `initStackDiagram` to the inline script**

  After `function initContactStack() { ... }` block and before `document.addEventListener('DOMContentLoaded', function() { initContactStack(); });`, insert this function (extracted verbatim from app.js lines 719–1013, with one fix: `renderStatic()` called at animation end):

  ```js
  function initStackDiagram() {
    var canvas = document.getElementById('stack-diagram');
    if (!canvas || !canvas.getContext) return;
    var ctx = canvas.getContext('2d');
    var dpr = window.devicePixelRatio || 1;
    var hasAnimated = false;
    var rafId = null;

    function colors() {
      var light = document.body.classList.contains('theme-light');
      return light ? {
        amber:'#b8922a',amberR:'184,146,42',greenR:'20,110,20',
        grid:'rgba(0,0,0,0.05)',shell:'rgba(0,0,0,0.10)',hdr:'rgba(0,0,0,0.28)',
        bracket:function(a){return 'rgba(0,0,0,'+a*0.28+')';},
        year:function(a){return 'rgba(0,0,0,'+a*0.35+')';}
      } : {
        amber:'#e8b84b',amberR:'232,184,75',greenR:'106,191,105',
        grid:'rgba(255,255,255,0.028)',shell:'rgba(255,255,255,0.07)',hdr:'rgba(255,255,255,0.18)',
        bracket:function(a){return 'rgba(255,255,255,'+a*0.22+')';},
        year:function(a){return 'rgba(255,255,255,'+a*0.32+')';}
      };
    }

    var FLOORS = [
      {label:'ARCHITECTURE',      years:'2009—2014',log1:'> INIT b.arch @ MNIT, Jaipur',         log2:'> BUILD spatial.systems.thinking'},
      {label:'ENTERPRISE SOFTWARE',years:'2015—2019',log1:'> DEPLOY erp.modules @ Tata, JSW',     log2:'> OPTIMIZE process.efficiency'},
      {label:'PRODUCT STRATEGY',  years:'2019—2023',log1:'> SHIP product.v1 @ JindalX, Blinkit', log2:'> INTEGRATE airtable.platform × 16'},
      {label:'AI STRATEGY',       years:'2023—PRESENT',log1:'> RUN ai_layer.init() @ present',    log2:'> SCALE ai.products → 7 deployed'}
    ];
    var BASE_H=[65,75,82,92];
    var TOTAL_BASE_H=BASE_H.reduce(function(s,h){return s+h;},0);
    var W,H,bX,bY,bW,bH,rects;

    function layout(){
      var rect=canvas.getBoundingClientRect();
      W=(rect.width||canvas.offsetWidth||440);H=(rect.height||canvas.offsetHeight||320);
      canvas.width=Math.round(W*dpr);canvas.height=Math.round(H*dpr);
      canvas.style.width=W+'px';canvas.style.height=H+'px';
      ctx.setTransform(dpr,0,0,dpr,0,0);
      bX=54;bY=38;bW=W-66;bH=H-58;
      var scale=bH/TOTAL_BASE_H;rects=[];var y=bY+bH;
      for(var i=0;i<4;i++){var fh=Math.round(BASE_H[i]*scale);y-=fh;rects.push({x:bX,y:y,w:bW,h:fh});}
    }

    var PHASE=680,GAP=260,LOG1_AT=300,LOG2_AT=820,CHAR_MS=20;
    var log1T=['','','',''],log2T=['','','',''],t0=null;

    function fp(i,el){return Math.max(0,Math.min((el-i*(PHASE+GAP))/PHASE,1));}
    function ease(t){return t<0.5?2*t*t:-1+(4-2*t)*t;}

    function drawGrid(){
      var C=colors();ctx.save();ctx.strokeStyle=C.grid;ctx.lineWidth=1;
      for(var x=0.5;x<W;x+=28){ctx.beginPath();ctx.moveTo(x,0);ctx.lineTo(x,H);ctx.stroke();}
      for(var y=0.5;y<H;y+=28){ctx.beginPath();ctx.moveTo(0,y);ctx.lineTo(W,y);ctx.stroke();}
      ctx.restore();
    }
    function drawHeader(){
      var C=colors();ctx.save();ctx.font='700 9px "Space Mono",monospace';ctx.fillStyle=C.hdr;
      ctx.fillText('BUILD LOG',bX,bY-14);ctx.textAlign='right';
      ctx.fillText('CAREER ARCHITECTURE',bX+bW,bY-14);ctx.textAlign='left';ctx.restore();
    }
    function drawShell(){
      var C=colors();ctx.save();ctx.strokeStyle=C.shell;ctx.lineWidth=1;ctx.setLineDash([4,7]);
      [[bX,bY,bX,bY+bH],[bX+bW,bY,bX+bW,bY+bH],[bX,bY+bH,bX+bW,bY+bH]].forEach(function(l){
        ctx.beginPath();ctx.moveTo(l[0],l[1]);ctx.lineTo(l[2],l[3]);ctx.stroke();
      });ctx.setLineDash([]);ctx.restore();
    }
    function drawFloor(i,p,elapsed){
      var C=colors(),r=rects[i],ep=ease(p);
      var sweepH=Math.round(r.h*ep),sweepTop=r.y+r.h-sweepH;
      if(sweepH>0){
        ctx.save();ctx.fillStyle='rgba('+C.amberR+','+(0.06+i*0.025)+')';
        ctx.fillRect(r.x+1,sweepTop,r.w-2,sweepH);
        if(p<1){var band=Math.min(18,sweepH);var g=ctx.createLinearGradient(0,sweepTop,0,sweepTop+band);
          g.addColorStop(0,'rgba('+C.amberR+',0.32)');g.addColorStop(1,'rgba('+C.amberR+',0)');
          ctx.fillStyle=g;ctx.fillRect(r.x+1,sweepTop,r.w-2,band);}ctx.restore();
      }
      if(p>0&&p<1&&Math.floor(elapsed/240)%2===0){
        ctx.save();ctx.strokeStyle=C.amber;ctx.lineWidth=2;ctx.setLineDash([5,5]);
        ctx.beginPath();ctx.moveTo(r.x+6,sweepTop);ctx.lineTo(r.x+r.w-6,sweepTop);
        ctx.stroke();ctx.setLineDash([]);ctx.restore();
      }
      if(p>=0.6){
        var lp=Math.min((p-0.6)/0.35,1);ctx.save();ctx.strokeStyle=C.amber;ctx.lineWidth=1;
        ctx.globalAlpha=lp;ctx.beginPath();ctx.moveTo(r.x,r.y);ctx.lineTo(r.x+r.w*lp,r.y);
        ctx.stroke();ctx.restore();
      }
      if(p>=0.68){
        var fa=Math.min((p-0.68)/0.28,1),fl=FLOORS[i];
        ctx.save();ctx.strokeStyle=C.bracket(fa);ctx.lineWidth=1;
        var bkx=r.x-20;
        ctx.beginPath();ctx.moveTo(bkx-3,r.y+3);ctx.lineTo(bkx,r.y+3);
        ctx.moveTo(bkx,r.y+3);ctx.lineTo(bkx,r.y+r.h-3);
        ctx.moveTo(bkx,r.y+r.h-3);ctx.lineTo(bkx-3,r.y+r.h-3);ctx.stroke();ctx.restore();
        ctx.save();ctx.font='700 8px "Space Mono",monospace';ctx.fillStyle='rgba('+C.amberR+','+(fa*0.65)+')';
        ctx.textAlign='right';ctx.fillText('F'+(i+1),r.x-6,r.y+r.h/2+3);ctx.textAlign='left';ctx.restore();
        ctx.save();ctx.font='700 9px "Space Mono",monospace';ctx.fillStyle='rgba('+C.amberR+','+fa+')';
        ctx.fillText(fl.label,r.x+8,r.y+13);ctx.restore();
        ctx.save();ctx.font='400 9px "Space Mono",monospace';ctx.fillStyle=C.year(fa);
        ctx.textAlign='right';ctx.fillText(fl.years,r.x+r.w-8,r.y+13);ctx.textAlign='left';ctx.restore();
      }
      var midY=r.y+Math.round(r.h*0.54);
      if(log1T[i]){ctx.save();ctx.font='400 10px "Space Mono",monospace';ctx.fillStyle='rgba('+C.greenR+',0.85)';ctx.fillText(log1T[i],r.x+8,midY);ctx.restore();}
      if(log2T[i]){ctx.save();ctx.font='400 10px "Space Mono",monospace';ctx.fillStyle='rgba('+C.greenR+',0.55)';ctx.fillText(log2T[i],r.x+8,midY+16);ctx.restore();}
    }
    function updateTyping(elapsed){
      for(var i=0;i<4;i++){
        var base=i*(PHASE+GAP);
        var e1=elapsed-(base+LOG1_AT),e2=elapsed-(base+LOG2_AT);
        if(e1>0)log1T[i]=FLOORS[i].log1.slice(0,Math.min(Math.floor(e1/CHAR_MS),FLOORS[i].log1.length));
        if(e2>0)log2T[i]=FLOORS[i].log2.slice(0,Math.min(Math.floor(e2/CHAR_MS),FLOORS[i].log2.length));
      }
    }
    function renderStatic(){
      var C=colors();ctx.clearRect(0,0,W,H);drawGrid();drawHeader();drawShell();
      for(var i=0;i<4;i++){
        var r=rects[i],fl=FLOORS[i];
        ctx.fillStyle='rgba('+C.amberR+','+(0.06+i*0.025)+')';ctx.fillRect(r.x+1,r.y,r.w-2,r.h);
        ctx.strokeStyle=C.amber;ctx.lineWidth=1;ctx.beginPath();ctx.moveTo(r.x,r.y);ctx.lineTo(r.x+r.w,r.y);ctx.stroke();
        ctx.font='700 9px "Space Mono",monospace';ctx.fillStyle=C.amber;ctx.fillText(fl.label,r.x+8,r.y+13);
        ctx.textAlign='right';ctx.font='400 9px "Space Mono",monospace';ctx.fillStyle=C.year(1);
        ctx.fillText(fl.years,r.x+r.w-8,r.y+13);ctx.textAlign='left';
        var midY=r.y+Math.round(r.h*0.54);
        ctx.font='400 10px "Space Mono",monospace';
        ctx.fillStyle='rgba('+C.greenR+',0.85)';ctx.fillText(fl.log1,r.x+8,midY);
        ctx.fillStyle='rgba('+C.greenR+',0.55)';ctx.fillText(fl.log2,r.x+8,midY+16);
      }
    }
    var themeObserver=new MutationObserver(function(){if(hasAnimated&&!rafId){layout();renderStatic();}});
    themeObserver.observe(document.body,{attributes:true,attributeFilter:['class']});

    function frame(ts){
      if(!t0)t0=ts;var el=ts-t0;
      ctx.clearRect(0,0,W,H);drawGrid();drawHeader();drawShell();
      for(var i=0;i<4;i++)drawFloor(i,fp(i,el),el);
      updateTyping(el);
      if(el<3*(PHASE+GAP)+PHASE+1600){rafId=requestAnimationFrame(frame);}
      else{rafId=null;renderStatic();} // ← ensure clean final state
    }
    function animate(){
      if(hasAnimated)return;hasAnimated=true;layout();t0=null;rafId=requestAnimationFrame(frame);
    }
    var replayBtn=document.getElementById('stack-replay');
    if(replayBtn){
      replayBtn.addEventListener('click',function(){
        if(rafId)cancelAnimationFrame(rafId);
        hasAnimated=false;log1T=['','','',''];log2T=['','','',''];animate();
      });
    }
    document.addEventListener('panel:activate',function(e){if(e.detail.panelId==='panel-home')animate();});
    if(document.getElementById('panel-home')&&document.getElementById('panel-home').classList.contains('is-active')){
      animate();
    }
  }
  ```

- [ ] **Step 2: Call `initStackDiagram()` inside the existing DOMContentLoaded handler**

  In the second `document.addEventListener('DOMContentLoaded', ...)` block (which currently only calls `initContactStack()`), add:
  ```js
  document.addEventListener('DOMContentLoaded', function() {
    initContactStack();
    initStackDiagram();
  });
  ```

---

## Task 3 — Fix nav-link-active amber-on-amber CTA (P0: Issue 24)

**Files:** Modify `index.html`

- [ ] **Step 1: Remove `nav-link` class from the "Get in touch" CTA button (line 336)**

  Change:
  ```html
  <button type="button" class="nav-link hidden md:block bg-primary-container text-on-primary font-label-md text-label-md px-4 py-2 uppercase tracking-widest hover:bg-primary-fixed transition-colors" data-panel="panel-contact">Get in touch</button>
  ```
  To:
  ```html
  <button type="button" class="hidden md:block bg-primary-container text-on-primary font-label-md text-label-md px-4 py-2 uppercase tracking-widest hover:bg-primary-fixed transition-colors" data-panel="panel-contact">Get in touch</button>
  ```

- [ ] **Step 2: Remove `nav-link` class from the logo button (line 322)**

  Change:
  ```html
  <button type="button" class="flex flex-col text-left nav-link" data-panel="panel-home">
  ```
  To:
  ```html
  <button type="button" class="flex flex-col text-left" data-panel="panel-home">
  ```

  Note: Both buttons still have `data-panel` so they still trigger panel navigation via the `[data-panel]` click handler — they just won't receive the active state styling.

---

## Task 4 — Fix home stats strip: flex → 2×2 grid on mobile (P1: Issue 4)

**Files:** Modify `index.html`

- [ ] **Step 1: Add class `home-stats` to the stats wrapper div (line 363)**

  Change:
  ```html
  <div class="flex flex-wrap items-center gap-8 border-t border-outline-variant pt-8">
  ```
  To:
  ```html
  <div class="home-stats flex flex-wrap items-center gap-8 border-t border-outline-variant pt-8">
  ```

- [ ] **Step 2: Add mobile CSS rule for `.home-stats`**

  Inside the `@media (max-width: 767px)` block in `<style>`, add:
  ```css
  .home-stats { display: grid !important; grid-template-columns: 1fr 1fr !important; gap: 20px !important; }
  .home-stats .w-px { display: none; }
  ```

---

## Task 5 — Skills + Philosophy: 1-column on mobile (P1: Issues 16, 18)

**Files:** Modify `index.html` inline `<style>`

- [ ] **Step 1: Change Skills and Philosophy mobile grid from 2-col to 1-col**

  In the `@media (max-width: 767px)` block, find:
  ```css
  #panel-skills .lg\:grid-cols-3 { grid-template-columns: repeat(2, 1fr); }
  #panel-philosophy .lg\:grid-cols-3 { grid-template-columns: repeat(2, 1fr); }
  ```
  Replace with:
  ```css
  #panel-skills .lg\:grid-cols-3 { grid-template-columns: 1fr; }
  #panel-philosophy .lg\:grid-cols-3 { grid-template-columns: 1fr; }
  ```

  Also reduce the minimum height on mobile so cells don't feel wasteful at full width:
  ```css
  #panel-skills .skill-block,
  #panel-philosophy .skill-block { min-height: 200px !important; }
  ```

---

## Task 6 — Projects: hide standalone arrow on mobile, make rows 07/08 interactive (P1·P2: Issues 11, 12)

**Files:** Modify `index.html`

- [ ] **Step 1: Add mobile CSS to hide standalone arrow column in project rows**

  In the `@media (max-width: 767px)` block, after existing `.project-row` rules, add:
  ```css
  .project-row .w-8 { display: none; }
  ```

- [ ] **Step 2: Make row 07 ("Portfolio Website") open a modal**

  Change row 07 from:
  ```html
  <div class="project-row group flex flex-col md:flex-row items-start md:items-center py-6 border-b border-outline-variant gap-4" style="cursor:default">
    <div class="w-10 font-label-md text-label-md text-on-surface-variant">07</div>
    <div class="flex-grow font-headline-md text-headline-md text-on-surface">Portfolio Website — This Site</div>
  ```
  To:
  ```html
  <div class="project-row group flex flex-col md:flex-row items-start md:items-center py-6 border-b border-outline-variant gap-4" onclick="openV3Modal('pp-1')">
    <div class="w-10 font-label-md text-label-md text-on-surface-variant">07</div>
    <div class="flex-grow font-headline-md text-headline-md text-on-surface group-hover:text-primary-fixed-dim transition-colors">Portfolio Website — This Site</div>
  ```
  
  Also add back the arrow div at the end of row 07:
  ```html
  <div class="w-8 flex justify-end"><span class="material-symbols-outlined arrow-icon transition-transform duration-200 text-on-surface-variant">arrow_forward</span></div>
  ```

- [ ] **Step 3: Make row 08 ("GWS CLI") open a modal**

  Change row 08 from:
  ```html
  <div class="project-row group flex flex-col md:flex-row items-start md:items-center py-6 border-b border-outline-variant gap-4" style="cursor:default">
    <div class="w-10 font-label-md text-label-md text-on-surface-variant">08</div>
    <div class="flex-grow font-headline-md text-headline-md text-on-surface">GWS CLI — Google Workspace Terminal Tool</div>
  ```
  To:
  ```html
  <div class="project-row group flex flex-col md:flex-row items-start md:items-center py-6 border-b border-outline-variant gap-4" onclick="openV3Modal('pp-3')">
    <div class="w-10 font-label-md text-label-md text-on-surface-variant">08</div>
    <div class="flex-grow font-headline-md text-headline-md text-on-surface group-hover:text-primary-fixed-dim transition-colors">GWS CLI — Google Workspace Terminal Tool</div>
  ```
  
  Also add back the arrow div at end of row 08:
  ```html
  <div class="w-8 flex justify-end"><span class="material-symbols-outlined arrow-icon transition-transform duration-200 text-on-surface-variant">arrow_forward</span></div>
  ```

  Note: `pp-3` already exists in `V3_PROJECTS` (GWS CLI entry with `githubUrl`).

---

## Task 7 — Fix Experience: inline timeline dots + mobile 1-column (P2: Issues 7, 10)

**Files:** Modify `index.html`

- [ ] **Step 1: Replace the separate spine column with inline dots in accordion headers**

  The current structure is a 2-column grid with a separate spine column. Replace the outer `grid` + entire spine `div` with a single-column layout where each accordion row has its own dot.

  **Current structure (replace this entire block, lines 388–534):**
  ```html
  <div class="grid grid-cols-[auto_1fr] gap-x-10 md:gap-x-20 items-start">
    <!-- Timeline spine -->
    <div class="relative flex flex-col items-center py-6">
      <div class="absolute top-0 bottom-0 w-px bg-outline-variant opacity-40"></div>
      <div class="flex flex-col gap-[220px] z-10">
        <div class="timeline-dot w-3 h-3 border border-background" style="background-color:#c9a84c;box-shadow:0 0 8px rgba(201,168,76,0.5)"></div>
        <div class="timeline-dot w-3 h-3 border border-background" style="background-color:#3a3a50"></div>
        <div class="timeline-dot w-3 h-3 border border-background" style="background-color:#3a3a50"></div>
        <div class="timeline-dot w-3 h-3 border border-background" style="background-color:#3a3a50"></div>
        <div class="timeline-dot w-3 h-3 border border-background" style="background-color:#3a3a50"></div>
      </div>
    </div>
    <!-- Accordion -->
    <div class="flex flex-col gap-10" id="experience-accordion">
      [5 accordion rows...]
    </div>
  </div>
  ```

  **New structure:**
  ```html
  <div class="relative" id="experience-accordion">
    <div class="absolute left-3 top-0 bottom-0 w-px bg-outline-variant opacity-40 hidden md:block"></div>
    [5 accordion rows with inline dot, see below...]
  </div>
  ```

  Each accordion row header gets a dot prepended. For each row, change the outer `<div class="accordion-row...">` and its header from this pattern:
  ```html
  <div class="flex flex-col md:flex-row md:items-center justify-between etched-border p-6 bg-surface-container-low group-hover:bg-surface-container transition-all">
  ```
  To:
  ```html
  <div class="flex items-start md:items-center gap-4 etched-border p-6 bg-surface-container-low group-hover:bg-surface-container transition-all">
    <div class="timeline-dot mt-2 md:mt-0 w-3 h-3 flex-shrink-0 border border-background hidden md:block" style="background-color:#3a3a50"></div>
    <div class="flex-1 flex flex-col md:flex-row md:items-center justify-between">
      [existing inner content]
    </div>
  ```
  
  The first row (Independent, `onclick="toggleExpAccordion(0)"`) gets dot with `style="background-color:#c9a84c;box-shadow:0 0 8px rgba(201,168,76,0.5)"` since it's the active role. All other dots use `style="background-color:#3a3a50"`.

  The `toggleExpAccordion` already updates dot colors via `document.querySelectorAll('.timeline-dot')` — it still works since we're keeping the `.timeline-dot` class.

- [ ] **Step 2: Remove the `grid-cols-[auto_1fr]` mobile CSS rule** that was targeting the old experience grid

  In the `@media (max-width: 767px)` block, delete:
  ```css
  .grid-cols-\[auto_1fr\] { grid-template-columns: 32px 1fr; gap: 16px; }
  .timeline-dot { width: 8px; height: 8px; }
  ```

  The `#experience-accordion` is now a single-column layout naturally.

---

## Task 8 — Hero headline size (P2: Issue 3)

**Files:** Modify `index.html` line 355

- [ ] **Step 1: Reduce hero headline from 60px to 48px on desktop**

  Change:
  ```html
  <h1 class="font-display text-[44px] md:text-[60px] leading-[1.0] text-on-surface mb-6 max-w-2xl">
  ```
  To:
  ```html
  <h1 class="font-display text-[36px] md:text-[48px] leading-[1.05] text-on-surface mb-6 max-w-2xl">
  ```

---

## Task 9 — Skills: add hierarchy (P2: Issue 14)

**Files:** Modify `index.html` lines 659–666

- [ ] **Step 1: Make "Product Strategy" (cell 01) span 2 columns on desktop**

  Change cell 01's outer div from:
  ```html
  <div class="skill-block delay-1 relative overflow-hidden p-8 border-r border-b border-outline-variant bg-surface-container-low min-h-[320px]">
  ```
  To:
  ```html
  <div class="skill-block delay-1 relative overflow-hidden p-8 border-r border-b border-outline-variant bg-surface-container-low min-h-[320px] lg:col-span-2">
  ```

  This creates a 2+1 first row (Product Strategy + AI & Builder Tools), then a second 1+1+1 row (Enterprise SaaS + Data & Analytics + Design & Prototyping), with Delivery & Process wrapping to a third row. To rebalance, also change cell 06 to span full row: add `lg:col-span-3 md:col-span-2` to cell 06.

- [ ] **Step 2: Add a bottom stat strip inside cell 01 to use the extra space**

  Inside cell 01, after the `</ul>`, add:
  ```html
  <div class="mt-6 pt-4 border-t border-outline-variant/30 flex gap-6">
    <div><span class="font-display text-[28px] text-primary-fixed-dim leading-none">8+</span><span class="font-label-sm text-label-sm text-on-surface-variant block mt-1 uppercase tracking-wider">Years</span></div>
    <div><span class="font-display text-[28px] text-primary-fixed-dim leading-none">30+</span><span class="font-label-sm text-label-sm text-on-surface-variant block mt-1 uppercase tracking-wider">Initiatives</span></div>
  </div>
  ```

---

## Task 10 — Philosophy: capstone amber accent (P2: Issue 17)

**Files:** Modify `index.html` lines 728–735

- [ ] **Step 1: Give capstone amber background + amber border-left**

  Change the capstone div from:
  ```html
  <div class="skill-block lg:col-span-3 md:col-span-2 col-span-1 relative overflow-hidden p-8 border-r border-b border-outline-variant bg-surface-container min-h-[200px]" style="animation-delay:0.7s">
  ```
  To:
  ```html
  <div class="skill-block lg:col-span-3 md:col-span-2 col-span-1 relative overflow-hidden p-8 border-r border-b border-outline-variant border-l-4 border-l-primary-fixed-dim min-h-[200px]" style="animation-delay:0.7s;background:rgba(201,168,76,0.08);">
  ```

- [ ] **Step 2: Make capstone title larger**

  Change capstone `h2` from:
  ```html
  <h2 class="font-headline-md text-headline-md text-primary-fixed-dim mb-6 uppercase">The Inevitability Test</h2>
  ```
  To:
  ```html
  <h2 class="font-headline-lg text-headline-lg text-primary-fixed-dim mb-6 uppercase">The Inevitability Test</h2>
  ```

---

## Task 11 — Credentials: remove hover affordance, add education content, rebalance split (P2: Issues 21, 22)

**Files:** Modify `index.html` lines 741–782

- [ ] **Step 1: Rebalance split from 50/50 to 40/60**

  Change the education side:
  ```html
  <div class="w-full md:w-1/2 bg-surface-container-low border-r border-outline-variant flex flex-col">
  ```
  To:
  ```html
  <div class="w-full md:w-2/5 bg-surface-container-low border-r border-outline-variant flex flex-col">
  ```

  Change the certifications side:
  ```html
  <div class="w-full md:w-1/2 bg-surface-container-lowest flex flex-col relative">
  ```
  To:
  ```html
  <div class="w-full md:w-3/5 bg-surface-container-lowest flex flex-col relative">
  ```

- [ ] **Step 2: Add a foundation note below the B.Arch entry**

  After the closing `</div>` of the B.Arch entry (after `<p class="font-label-sm...opacity-60 mt-2">PERIOD: 2009 — 2014</p></div>`), add:
  ```html
  <div class="mt-8 p-6 border border-outline-variant/30 bg-surface-container-low/30">
    <p class="font-label-sm text-label-sm text-primary-fixed-dim uppercase tracking-wider mb-3">Foundation</p>
    <p class="font-body-md text-body-md text-on-surface-variant leading-relaxed">5 years studying how systems serve human needs at scale — spatial logic, load paths, site constraints — the same discipline that shapes product architecture thinking today.</p>
  </div>
  ```

- [ ] **Step 3: Remove hover state from all cert rows**

  All cert row inner divs have this pattern:
  ```html
  class="bg-surface-container-lowest p-4 flex justify-between items-center group hover:bg-surface-container-low transition-colors"
  ```
  Replace `group hover:bg-surface-container-low transition-colors` with nothing (remove those three classes from every cert row div). Use replace-all since every cert row has this same class string.

  Also in the `h4` elements inside cert rows, remove `group-hover:text-primary-fixed-dim transition-colors`:
  ```html
  <h4 class="font-body-md text-body-md text-on-surface group-hover:text-primary-fixed-dim transition-colors">
  ```
  Becomes:
  ```html
  <h4 class="font-body-md text-body-md text-on-surface">
  ```

---

## Task 12 — Contact: hide canvas on mobile (P2: Issue 27)

**Files:** Modify `index.html`

- [ ] **Step 1: Hide contact canvas container on mobile via existing responsive classes**

  Change the contact canvas wrapper div from:
  ```html
  <div class="md:col-span-5 flex justify-center md:justify-end mt-12 md:mt-0">
  ```
  To:
  ```html
  <div class="hidden md:flex md:col-span-5 justify-center md:justify-end mt-12 md:mt-0">
  ```

  Also remove the now-irrelevant mobile canvas resize rule from `@media (max-width: 767px)`:
  ```css
  #panel-contact canvas { width: 200px !important; height: 200px !important; }
  ```

---

## Task 13 — Standardize Experience panel header (P2: Issue 29)

**Files:** Modify `index.html` lines 384–387

- [ ] **Step 1: Add amber left border to Experience header to match Projects/Skills/Philosophy**

  Change:
  ```html
  <div class="mb-16">
    <h3 class="font-label-sm text-label-sm text-primary-fixed-dim uppercase tracking-[0.2em] mb-4">Professional Trajectory &amp; Strategic Output</h3>
    <h1 class="font-display text-display text-on-surface">Experience &amp; Work</h1>
  </div>
  ```
  To:
  ```html
  <div class="mb-16 border-l-2 border-primary-container pl-6">
    <h3 class="font-label-sm text-label-sm text-primary-fixed-dim uppercase tracking-[0.2em] mb-4">Professional Trajectory &amp; Strategic Output</h3>
    <h1 class="font-display text-display text-on-surface">Experience &amp; Work</h1>
  </div>
  ```

---

## Task 14 — Commit

- [ ] **Step 1: Verify no console errors by reopening in browser (check devtools)**

- [ ] **Step 2: Commit all changes**

  ```bash
  git add index.html
  git commit -m "fix: 22 P0/P1/P2 QA issues — remove app.js, fix CTA, mobile layouts, hierarchy, credentials"
  ```

---

## Self-Review

**Spec coverage check:**
- P0·1 (classList crash) → Task 1 removes app.js ✓
- P0·24 (amber-on-amber CTA) → Task 3 ✓
- P1·2,8 (duplicate cards) → Task 1 removes app.js ✓
- P1·4 (home stats mobile) → Task 4 ✓
- P1·11 (project rows mobile) → Task 6 ✓
- P1·16,18 (skills/philosophy 1-col mobile) → Task 5 ✓
- P1·20 (cert count) → Verified `[13]` is correct (2+3+3+5=13), no fix needed ✓
- P1·28 (two modal systems) → Task 1 removes `#modal-overlay` ✓
- P2·3 (hero headline 60px) → Task 8 ✓
- P2·5 (stack canvas > SCA) → Task 2 adds `renderStatic()` at animation end ✓
- P2·7 (timeline dots drift) → Task 7 ✓
- P2·10 (mobile experience gutter) → Task 7 ✓
- P2·12 (rows 07/08 dead hover) → Task 6 steps 2+3 ✓
- P2·14 (skills flat grid) → Task 9 ✓
- P2·17 (capstone weak) → Task 10 ✓
- P2·19 (philosophy mobile overlap) → Resolved by Task 5 (1-col); capstone spans full width naturally ✓
- P2·21 (cert hover no click) → Task 11 step 3 ✓
- P2·22 (education empty) → Task 11 steps 1+2 ✓
- P2·25 (dead contact canvas) → Task 1 step 4 ✓
- P2·27 (mobile contact canvas) → Task 12 ✓
- P2·29 (inconsistent headers) → Task 13 (Experience) ✓

**Stack diagram dependency:** `initStackDiagram` calls `document.getElementById('stack-diagram')` which exists in HTML. The `panel:activate` event is dispatched in the inline `activatePanel` function. Dependency chain is intact.

**`toggleExpAccordion` still works:** It selects `.timeline-dot` and `.accordion-row` — both classes still present after Task 7 restructure. ✓
